<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

<h1>Books</h1>
<table>
<tr>
<th><span>Title</span></th>
<th><span>Body</span></th>
</tr>
<% @lists.each do |list| %> 

<tr>
<td><%= list.title %>
</td>
<td><%= list.body %>
</td>
<td><%=link_to 'Show', book_path(list.id) %>
</td>
<td><%= link_to "Edit",edit_book_path(list.id) %>
</td>
<td><%= link_to "Destroy", book_path(list.id), method: :delete, "data-confirm" => "Are you sure?" %>
</td>
</tr>


<% end %>
</table>



   <h1>New book</h1>
<!-- form_for部分 -->
<%= form_for(@list,url: books_path) do |f| %>
 
   <% if @list.errors.any? %>
   <%= @list.errors.count %>errors prohibited this book from being saved
   <% @list.errors.full_messages.each do |message| %>
   <%= message %>
   <% end %>
   <% end %>

    <h4>Title</h4>
    <%= f.text_field :title %>
 
    <h4>Body</h4>
    <%= f.text_area :body %>
 
    <%= f.submit 'Create Book' %>

      
   

<% end %>

   